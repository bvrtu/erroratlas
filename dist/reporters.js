export function renderConsole(result, diagnostics = result.diagnostics) {
    const lines = [];
    for (const diagnostic of diagnostics) {
        const location = diagnostic.location
            ? `${diagnostic.location.file}:${diagnostic.location.line}:${diagnostic.location.column}`
            : "project";
        lines.push(`${severityMark(diagnostic.severity)} ${location} [${diagnostic.ruleId}] ${diagnostic.message}`);
    }
    const structured = result.errors.filter((item) => item.structured).length;
    const counts = countSeverities(diagnostics);
    if (lines.length)
        lines.push("");
    lines.push(`Scanned ${result.filesScanned} files · ${structured} structured errors · ` +
        `${counts.error} errors · ${counts.warning} warnings · ${counts.note} notes`);
    return `${lines.join("\n")}\n`;
}
export function renderMarkdown(catalog) {
    const lines = [
        "# Error catalog",
        "",
        "<!-- Generated by ErrorAtlas. Edit descriptions and resolutions in erroratlas.catalog.json. -->",
        "",
        `Generated: ${catalog.generatedAt}`,
        "",
        `Documented errors: **${catalog.errors.length}**`,
        "",
    ];
    for (const error of catalog.errors) {
        lines.push(`## \`${escapeMarkdown(error.code)}\``, "");
        lines.push(`- **HTTP status:** ${error.status ?? "—"}`);
        lines.push(`- **Message:** ${error.message ? `\`${escapeMarkdown(error.message)}\`` : "—"}`);
        if ((error.observedMessages?.length ?? 0) > 1) {
            lines.push("- **Observed messages:**");
            for (const message of error.observedMessages ?? []) {
                lines.push(`  - \`${escapeMarkdown(message)}\``);
            }
        }
        lines.push(`- **Description:** ${error.description || "_Not documented yet._"}`);
        lines.push(`- **Resolution:** ${error.resolution || "_Not documented yet._"}`, "");
        if (error.occurrences.length) {
            lines.push("**Defined at**", "");
            for (const occurrence of error.occurrences) {
                lines.push(`- \`${occurrence.file}:${occurrence.line}\` (${occurrence.language}, ${occurrence.constructor}${occurrence.flow ? `, ${occurrence.flow}` : ""})`);
            }
            lines.push("");
        }
    }
    return `${lines.join("\n").trimEnd()}\n`;
}
export function renderSarif(diagnostics) {
    const ruleIds = [...new Set(diagnostics.map((item) => item.ruleId))].sort();
    const sarif = {
        version: "2.1.0",
        $schema: "https://json.schemastore.org/sarif-2.1.0.json",
        runs: [
            {
                tool: {
                    driver: {
                        name: "ErrorAtlas",
                        informationUri: "https://github.com/bvrtu/erroratlas",
                        rules: ruleIds.map((id) => ({
                            id,
                            shortDescription: { text: humanize(id) },
                        })),
                    },
                },
                results: diagnostics.map((item) => sarifResult(item)),
            },
        ],
    };
    return `${JSON.stringify(sarif, null, 2)}\n`;
}
export function shouldFail(diagnostics, threshold) {
    if (threshold === "warning") {
        return diagnostics.some((item) => item.severity === "error" || item.severity === "warning");
    }
    return diagnostics.some((item) => item.severity === "error");
}
function sarifResult(diagnostic) {
    const result = {
        ruleId: diagnostic.ruleId,
        level: diagnostic.severity === "warning"
            ? "warning"
            : diagnostic.severity === "note"
                ? "note"
                : "error",
        message: { text: diagnostic.message },
    };
    if (diagnostic.location) {
        result.locations = [
            { physicalLocation: physicalLocation(diagnostic.location) },
        ];
    }
    return result;
}
function physicalLocation(location) {
    return {
        artifactLocation: { uri: location.file },
        region: {
            startLine: location.line,
            startColumn: location.column,
            endLine: location.endLine,
            endColumn: location.endColumn,
        },
    };
}
function countSeverities(diagnostics) {
    const counts = { error: 0, warning: 0, note: 0 };
    for (const item of diagnostics)
        counts[item.severity] += 1;
    return counts;
}
function severityMark(severity) {
    if (severity === "error")
        return "✖";
    if (severity === "warning")
        return "⚠";
    return "·";
}
function escapeMarkdown(value) {
    return value.replace(/`/g, "\\`").replace(/\|/g, "\\|");
}
function humanize(value) {
    return value
        .replace(/-/g, " ")
        .replace(/^./, (character) => character.toUpperCase());
}
//# sourceMappingURL=reporters.js.map